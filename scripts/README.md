# scripts/

| File | Purpose |
|------|---------|
| `launch-game-without-steam.bat` | Start RetroArch+content outside Steam (edit paths) |
| `PadTest.cs` / `.bat` | Live LX/LY/RX/RY for 10s |
| `CaptureSteps.cs` / `.bat` | Guided step log → `capture_steps.txt` |

Build needs Windows .NET Framework 4.x `csc.exe` (usually already present).

Do not commit `*.exe` or `capture_steps.txt` with personal noise — see repo `.gitignore`.
